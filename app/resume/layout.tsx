import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Vivek_SL_UI_UX_Growth_Designer",
};

export default function ResumeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
